export class WordCl {
  constructor(word) {
    this.id = id;
    this.word = word;
    this.status = status;
  }

  showMeaning() {} // GuiManager
  changeStatus() {} // DB (Update) and GUI (changeColor)
  hide() {} // (Gui)
  filter() {} // (Gui)
}
